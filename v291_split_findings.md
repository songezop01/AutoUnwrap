# v2.9.1 分割檔密碼診斷

## 報告版本

使用者提供的報告仍由 `2.7.0-diagnostics` 產生，因此其中沒有 v2.9.0 的最終遊戲目錄停止與巢狀補充密碼流程。

## PC13338 案例

第一層 `PC13338.7z.001` 被正確辨識為兩卷 7Z，使用 6 字元候選成功，約 10 秒完成並產生 `PC13338.jpg`。手動拖入後，程式辨識 JPG 內容為 RAR 並改名 `PC13338.rar`。

之後 7-Zip 對 `PC13338.rar` 的三個候選都在 0 秒內回報 `Cannot open encrypted archive. Wrong password?`，沒有產生任何輸出，並記錄 `entries=no`。這不是大檔案 CRC 部分成功，也不是分割卷遺漏的證據；在該報告中，最直接的結論是第二層的正確密碼沒有出現在原有候選，或該偽裝 RAR 不是完整可讀的 RAR。使用者手動拖入第二層時，舊版也沒有在所有候選失敗後要求補充密碼。

## v2.9.1 修正

1. 任何層級的候選全部失敗後都會發出 `PASSWORD_REQUEST`，包括手動拖入第二層而使 `depth=0` 的情況。
2. 使用者輸入的補充密碼只會單獨重試該層，不會重新重跑所有既有候選，避免大型檔案重複 I/O。
3. 成功的補充密碼加入後續層候選。
4. Bandizip 使用 `-p:password`；7-Zip 使用 `-ppassword`，兩者參數不混用。
5. 保留完整工具 stdout／stderr 及 `PASSWORD_REQUEST` 事件，便於判斷補充密碼後仍失敗時是密碼問題、RAR 完整性問題或工具相容性問題。
