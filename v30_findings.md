# Auto Unwrap v3.0 功能優化分析

## 影片中的 UI 問題

錄影顯示主視窗在最大化、還原及拖曳邊緣縮放時，外框會改變，但內部按鈕、日誌區及狀態區仍停留在建立時的絕對座標。v2.9.1 的 `WM_SIZE` 只重排少數控制項，因此產生大片空白與版面不隨視窗變化的問題。

v3.0 將主要控制項保存為全域 handle，統一由 `LayoutMainWindow` 在 `WM_SIZE` 中重排。正常寬度排列成一列；窄視窗自動改為三欄兩列，日誌、進度列與狀態列使用剩餘 client area。`WM_DPICHANGED` 會重新計算 DPI、重建 Segoe UI 字型並再次佈局。

## 輸出／刪除設定記憶

v2.9.1 雖然已有分散的 dat 設定檔，但為了使設定保存更容易驗證，v3.0 增加 `%APPDATA%\\AutoUnwrap\\settings.ini`。INI 的 `engine`、`output_mode`、`output_folder` 及 `delete_after` 是 v3.0 的權威副本，舊 dat 檔仍作為第一次升級時的 fallback。輸出設定視窗按下確定後會同步寫入兩種格式，啟動時會重新讀取，因此「完成後自動刪除」不會因重啟遺失。

## 帶附加說明文字的偽裝檔名

對 `XXXXX.rar請刪除文字後解壓`，最安全的方案不是把所有未知檔案直接改成壓縮檔，而是先用檔案內容辨識確認標頭，再清理檔名。v3.0 支援兩種方式：使用者在「偽裝檔名規則」視窗保存固定文字；若檔名內含 `.rar`、`.zip`、`.7z` 或 `.lz4` 後綴並接自然語言文字，程式也會自動截取正式後綴。確認內容不是壓縮檔時不改名，避免誤傷正常遊戲資源。

自訂規則保存於 `%APPDATA%\\AutoUnwrap\\disguise_rules.dat`，內容不是密碼，因此可用 UTF-16 直接保存。每次規則匹配都會寫入 `FILENAME_RULE` 診斷事件。

## 驗證範圍

已完成 Windows x64 交叉編譯與現有多層解壓、偽裝副檔名、分割檔、CRC、DPI 及診斷測試；新增契約測試覆蓋 WM_SIZE 佈局、設定保存、規則保存及「先確認內容才改名」。目前建置環境不是 Windows，不能直接啟動 GUI 進行滑鼠縮放或 Bandizip 實機驗證，因此實機首次測試仍應確認主視窗最大化／還原、固定輸出資料夾重啟後仍存在，以及自訂規則匹配後的診斷事件。
