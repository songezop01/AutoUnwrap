# Auto Unwrap v2.9 實機診斷分析

## 大檔案速度

診斷紀錄顯示第一層 `AA5560.rar` 解壓工具本身在約 1 分 25 秒完成，進度達到 99%，輸出約 5 GB。真正的額外延遲發生在解壓完成後：舊版 `NESTED_SCAN` 記錄 `direct_changed=3560`、`recursive_new=3560`，接著逐一掃描與辨識 3,560 個檔案，並花費約 3 分鐘。這不是壓縮引擎在解壓，而是 Auto Unwrap 以全目錄遞迴方式重新掃描輸出內容。

v2.9 以 `MoveTree` 回傳的直接檔案清單作為主要候選來源，只有清單為空才回掃輸出資料夾；暫存活動大小掃描間隔由 5 秒提高至 15 秒，降低大檔案處理期間的重複 I/O。

## 正常遊戲檔案被誤判

舊版 `DetectArchive` 對所有副檔名都可能搜尋前 64 KiB／1 MiB 的 ZIP／7Z／RAR 標頭。診斷紀錄因此把 `陨落之后v0.4_2.apk`、`1-1-LT1_2.save`、`auto-1-LT1_2.save`、`__init___2.pyc`、`zipimport_2.pyc` 改成 ZIP 並排入第二層工作佇列。這正是使用者第二張截圖中大量未知檔案的主要來源。

v2.9 只允許 JPG／PNG／GIF／BMP／WEBP／LZ4／DAT／IMG／TMP／無副檔名等可疑偽裝副檔名進行深度掃描；APK、PY、PYC、SAVE、XML、JSON、PROPERTIES 等正常遊戲檔案不再因內容中偶然出現 `PK` 而被改名。

## 最終遊戲目錄停止條件

如果本層新產生的檔案位於 `game` 或 `renpy` 目錄，或本層新產生任何 `.exe`，v2.9 會寫入 `FINAL_GAME_MARKER` 並停止該遊戲樹後續的巢狀掃描。原始檔案仍會保留，只有明確成功且沒有完整性警告時才符合自動刪除條件。

## 小型分割檔的第二層密碼

診斷紀錄顯示第一層 `PC13338.7z.001` 正常解壓約 10 秒，產生 `PC13338.jpg`；程式正確辨識為 RAR 並改名為 `PC13338.rar`。第二層三個現有候選都回報 `Cannot open encrypted archive. Wrong password?`，沒有輸出檔案。因此這不是 CRC 部分成功，而是該第二層密碼不在現有候選、第二層檔案本身損壞／不完整，或 7-Zip 無法讀取該 RAR。

v2.9 在巢狀層現有候選全失敗後彈出補充密碼視窗，只用新輸入密碼重試，不會重新解壓所有舊候選；若成功，新密碼會加入後續層候選。這讓不同層使用不同密碼的情況可以直接處理。
